apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: remote-control-envoy-gateway
  namespace: remote-control
spec:
  project: default
  source:
    helm:
      skipCrds: true
    chart: gateway-helm
    repoURL: docker.io/envoyproxy
    targetRevision: v1.8.0
  destination:
    namespace: remote-control
    server: https://kubernetes.default.svc
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true
