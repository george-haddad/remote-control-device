apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: remote-control-devices
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/george-haddad/remote-control-device.git"
    targetRevision: HEAD
    path: "k8s"
  #    directory:
  #      recurse: true
  destination:
    server: "https://kubernetes.default.svc"
    namespace: devices
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
